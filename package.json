{
  "name": "tracechat",
  "version": "1.0.0",
  "private": true,
  "description": "AI chat workspace with LLM inference tracing and ingestion.",
  "workspaces": [
    "apps/server",
    "apps/client"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm --workspace apps/server run dev",
    "dev:client": "npm --workspace apps/client run dev",
    "build": "npm --workspace apps/server run build && npm --workspace apps/client run build",
    "lint": "npm --workspace apps/server run lint && npm --workspace apps/client run lint",
    "db:generate": "npm --workspace apps/server run db:generate",
    "db:migrate": "npm --workspace apps/server run db:migrate",
    "db:studio": "npm --workspace apps/server run db:studio"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
